
// import { SetUserName, SetPassword, AddProduct, EditPorduct, DeleteProduct, SetDate,SetUser } from './Consts';


// const createAction = (type, payload) => {
//     return {
//         type: type,
//         payload: payload
//     }
// };

// export const setUserName = (userName) => createAction(SetUserName, userName);
// export const setPassword = (password) => createAction(SetPassword, password);
// export const addProduct = (product) => createAction(AddProduct, product);
// export const editPorduct = (product) => createAction(EditPorduct, product);
// export const deleteProduct = (productId) => createAction(DeleteProduct, productId);
// export const setDate = (date) => createAction(SetDate, date);
// export const setUser = (user) => createAction(SetUser, user);